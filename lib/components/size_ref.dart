import 'package:flutter/material.dart';

class sizeRef extends StatelessWidget {
  const sizeRef({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return Container();
  }
}
